# Claim 6 evaluation contract

The fixed command exits nonzero unless the parent strictly benefits, the
children are full-support, the compatible split reconstructs the parent, the
global pool remains unchanged at every sweep point, a child becomes strictly
harmed, the asymptotic certificate applies, the independent checker agrees,
both controls behave as intended, and Claims 1-5 still pass.

The verdict is the theorem's existential `VERIFIED`, not a claim that benefit
fails to propagate for every decomposition.
